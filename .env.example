# Soci Environment Variables
# Copy this file to .env.local for local development

# ===========================================
# Authentication
# ===========================================
# Password hash for login (generate with: console.log(await crypto.subtle.digest('SHA-256', new TextEncoder().encode('your-password')).then(b => Array.from(new Uint8Array(b)).map(b => b.toString(16).padStart(2, '0')).join('')))
VITE_APP_HASH=your_sha256_password_hash_here

# ===========================================
# Twitter/X OAuth 2.0 (for Vercel serverless functions)
# ===========================================
# Get these from https://developer.twitter.com/
TWITTER_CLIENT_ID=your_twitter_client_id
TWITTER_CLIENT_SECRET=your_twitter_client_secret
TWITTER_REDIRECT_URI=https://your-app.vercel.app/api/auth/twitter/callback

# ===========================================
# AI API Keys (stored in browser, entered via UI)
# ===========================================
# These are NOT environment variables - users enter them in Automation settings
# ANTHROPIC_API_KEY - For Claude content generation
# OPENAI_API_KEY - For DALL-E image generation
